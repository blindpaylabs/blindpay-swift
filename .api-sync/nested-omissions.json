[
  {
    "schema": "CustomerOut",
    "field": "aml_hits",
    "reason": "SDK models this shape (Customer.swift's AmlHits struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry, so its own fields are not mechanically field-diffed. Recorded here rather than wired in during this coverage-check PR, to avoid pulling an unrelated field-sync diff (and its own nested sub-shapes) into an infra-only change.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CustomerOut",
    "field": "fraud_warnings",
    "reason": "SDK models this shape (Customer.swift's FraudWarning struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR, to avoid pulling an unrelated field-sync/nullability diff into an infra-only change.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CustomerOut",
    "field": "kyc_warnings",
    "reason": "SDK models this shape (Customer.swift's KYCWarning struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR, to avoid pulling an unrelated field-sync diff into an infra-only change.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CustomerOut",
    "field": "limit",
    "reason": "SDK models this shape (Customer.swift's CustomerLimits struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR, to avoid pulling an unrelated field-sync diff into an infra-only change.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CustomerOut",
    "field": "owners",
    "reason": "SDK models this shape (Customer.swift's CustomerOwner struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR, to avoid pulling an unrelated field-sync diff into an infra-only change.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CreateCustomerIn",
    "field": "owners",
    "reason": "Same CustomerOwner shape as CustomerOut.owners; not wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "UpdateCustomerIn",
    "field": "owners",
    "reason": "Same CustomerOwner shape as CustomerOut.owners; not wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "BankAccountOut",
    "field": "offramp_wallets",
    "reason": "SDK models this shape (OfframpWallet.swift's OfframpWallet struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR, to avoid pulling an unrelated field-sync diff into an infra-only change.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "GetCustomerLimitsOut",
    "field": "limits",
    "reason": "SDK models this shape (CustomerLimits.swift's CustomerLimitsDetail struct, itself with further-nested payin/payout sub-shapes) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "VirtualAccountOut",
    "field": "blockchain_wallet",
    "reason": "SDK models this shape (VirtualAccount.swift's BlockchainWalletRef struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "VirtualAccountOut",
    "field": "us",
    "reason": "SDK models this shape (VirtualAccount.swift's VirtualAccountUS struct, itself with further-nested ach/beneficiary/receiving_bank/rtp/swift_intermediary_bank/swift_receiving_bank/wire sub-shapes -- some of which the SDK does not yet model at all, e.g. swift_intermediary_bank/swift_receiving_bank/swift_account_number) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR, since wiring it in would also surface those pre-existing field gaps as new sync findings.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "QuoteOut",
    "field": "contract",
    "reason": "SDK models this shape (Quote.swift's QuoteContract struct, itself with further-nested abi/network sub-shapes) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CreatePayinOut",
    "field": "blindpay_bank_details",
    "reason": "SDK models this shape (Payin.swift's BlindPayBankDetails struct, itself with further-nested ach/wire/rtp/beneficiary/receiving_bank sub-shapes and a pre-existing gap: no swift_receiving_bank/swift_account_number field) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CreatePayinOut",
    "field": "tracking_complete",
    "reason": "SDK models this shape (Payin.swift's PayinTrackingComplete struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry (the PayinOut.tracking_complete anchor exists for the optional variant; this is the CreatePayinOut non-optional response shape). Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CreatePayinOut",
    "field": "tracking_payment",
    "reason": "SDK models this shape (Payin.swift's PayinTrackingPayment struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry for the CreatePayinOut response shape. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CreatePayinOut",
    "field": "tracking_transaction",
    "reason": "SDK models this shape (Payin.swift's PayinTrackingTransaction struct, itself with further-nested pse_instruction/ted_instruction/transfers_instruction sub-shapes the SDK does not yet model) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry for the CreatePayinOut response shape. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "PayinOut",
    "field": "blindpay_bank_details",
    "reason": "SDK models this shape (Payin.swift's BlindPayBankDetails struct, itself with further-nested ach/wire/rtp/beneficiary/receiving_bank sub-shapes and a pre-existing gap: no swift_receiving_bank/swift_account_number field) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "PayoutOnEvmOut",
    "field": "tracking_complete",
    "reason": "SDK models this shape (Payout.swift's PayoutTrackingComplete struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry for the PayoutOnEvmOut response shape. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "PayoutOnEvmOut",
    "field": "tracking_liquidity",
    "reason": "SDK models this shape (Payout.swift's PayoutTrackingLiquidity struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry for the PayoutOnEvmOut response shape. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "PayoutOnEvmOut",
    "field": "tracking_transaction",
    "reason": "SDK models this shape (Payout.swift's PayoutTrackingTransaction struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry for the PayoutOnEvmOut response shape. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CreateTransferOut",
    "field": "tracking_bridge_swap",
    "reason": "SDK models this shape (Transfer.swift's TransferTrackingStep struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry for the CreateTransferOut response shape. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CreateTransferOut",
    "field": "tracking_complete",
    "reason": "SDK models this shape (Transfer.swift's TransferTrackingStep struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry for the CreateTransferOut response shape. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CreateTransferOut",
    "field": "tracking_paymaster",
    "reason": "SDK models this shape (Transfer.swift's TransferTrackingStep struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry for the CreateTransferOut response shape. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "CreateTransferOut",
    "field": "tracking_transaction_monitoring",
    "reason": "SDK models this shape (Transfer.swift's TransferTrackingTransactionMonitoring struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry for the CreateTransferOut response shape. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "TransferOut",
    "field": "tracking_bridge_swap",
    "reason": "SDK models this shape (Transfer.swift's TransferTrackingStep struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "TransferOut",
    "field": "tracking_complete",
    "reason": "SDK models this shape (Transfer.swift's TransferTrackingStep struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "TransferOut",
    "field": "tracking_paymaster",
    "reason": "SDK models this shape (Transfer.swift's TransferTrackingStep struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "TransferOut",
    "field": "tracking_transaction_monitoring",
    "reason": "SDK models this shape (Transfer.swift's TransferTrackingTransactionMonitoring struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  },
  {
    "schema": "AvailableBankDetails",
    "field": "items",
    "reason": "SDK models this shape (Available.swift's BankDetailItem struct) but it isn't wired into spec-map.json's types[] as its own dotted-locator entry. Recorded here rather than wired in during this coverage-check PR.",
    "owner": "eric@blindpay.com"
  }
]
